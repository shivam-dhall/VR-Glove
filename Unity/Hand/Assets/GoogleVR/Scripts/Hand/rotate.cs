using UnityEngine;
using System.Collections;

public class rotate : MonoBehaviour {

    GameObject obj;
    GameObject refer;

	// Use this for initialization
	void Start () {
        //obj = GameObject.Find("Cylinder1");
        //refer = GameObject.Find("Sphere");
	}
	
	// Update is called once per frame
	void Update () {
        //obj.transform.RotateAround(refer.transform.position, Vector3.right, Time.deltaTime * 10);
	}
}
